/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['drive.google.com', 'www.italyperfect.com', 'a.eu.mktgcdn.com', 'www.seniorsregion.fr'],
        // Добавляем домен Google Drive
    },
};

export default nextConfig;
